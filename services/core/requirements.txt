fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic[email]==2.10.0
psycopg[binary,pool]==3.2.3
redis[asyncio]==5.2.0
PyJWT==2.10.0
bcrypt==4.2.0
numpy==2.2.0
httpx==0.28.0
python-multipart==0.0.19
minio==7.2.12
reportlab==4.1.0
pdfplumber==0.11.4
# Optional observability. Activated only when SENTRY_DSN is set. Importing
# is guarded inside services/shared/base.py so absence never breaks boot,
# but pinning the dep here keeps the image reproducible when the DSN is on.
sentry-sdk[fastapi]==2.16.0
